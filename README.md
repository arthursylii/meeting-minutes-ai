# Meeting Minutes AI

iPhone-first PWA for recording meetings, transcription, AI minutes, action items and meeting history.

Deploy this repository as a Docker web service with an OPENAI_API_KEY secret. Open the HTTPS URL in iPhone Safari and use Share -> Add to Home Screen.
